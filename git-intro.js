//Git:
//version control system.
//created by linus

//Repository (Repo):
//Project folder/place where the project is kept.

// local vs remote

//Github/Gitlab(free): a website to host your repo online.


/*******************************************************************************************************/
// ------------------------
// to check version of your git system:
// git --version

// to clone a repo hosted in github/gitlab into your local laptop:
// git clone


// to create a new Git repo:
// git init (give anything to this hidden git folder)

// to add the files except the files in .gitignore (to the staging area) for tracking:
// git add .

// to save the change
// git commit -m "write sth here"

// git push --set-upstream origin jc-1.0-20220207

// to create a branch/version
// git checkout -b 'jt-branch-version1.0'

//switch between branches-git checkout
//view all branches-git branch --all

// fffff
// built a carousel

// git: 纵向命令。横向命令。写简历但是不考


// Workflow of a project

// 1. clone your project
// git clone git@gitlab.com:jchen74/fetch-city-view.git

//2. create your own branch!!!
//git checkout -b your-branch-name
// (-b it means branch)

//3. edit/add your code

//4. save and push your branch to the project
//git add .
//git commit -m "add sth"
//git push
//copy and paste the notification. e.g: git push --set-upstream origin jc-1.0-20220207

//5. merge request




















